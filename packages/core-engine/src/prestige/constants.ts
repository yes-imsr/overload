/** Minimum Credits required to start a prestige attempt. */
export const PRESTIGE_REQUIRED_CREDITS = 250;

/** Credits removed when a prestige attempt fails. */
export const PRESTIGE_FAILURE_CREDIT_PENALTY = 50;

/** Hours locked out after a failed prestige attempt. */
export const PRESTIGE_FAILURE_LOCKOUT_HOURS = 72;

/** Successful attempts must beat the prior best e1RM by this ratio. */
export const PRESTIGE_E1RM_IMPROVEMENT_RATIO = 1.02;

/** Minimum calibrated exercises required for prestige eligibility. */
export const PRESTIGE_MIN_CALIBRATED_EXERCISES = 1;
