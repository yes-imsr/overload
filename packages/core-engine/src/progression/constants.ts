/** Weight increase for Easy effort (5%). */
export const WEIGHT_INCREASE_RATIO_EASY = 0.05;

/** Weight increase for Medium effort (2.5%). */
export const WEIGHT_INCREASE_RATIO_MEDIUM = 0.025;

/** Weight decrease for Near Death effort (2.5%). */
export const WEIGHT_DECREASE_RATIO_NEAR_DEATH = 0.025;

export const REP_INCREASE_EASY = 2;
export const REP_INCREASE_MEDIUM = 1;

/** Rep cap reduction when Near Death caps next session. */
export const NEAR_DEATH_REP_CAP_REDUCTION = 2;

/** Consecutive struggle sessions before plateau is flagged. */
export const PLATEAU_FAILED_SESSION_THRESHOLD = 3;
